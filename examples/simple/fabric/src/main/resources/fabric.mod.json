{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${version}",
  "name": "${name}",
  "description": "${description}",
  "authors": [
    "${author}"
  ],
  "contact": {
    "homepage": "${homepage}",
    "issues": "${issues}",
    "sources": "${sources}"
  },
  "license": "${license}",
  "icon": "${icon}",
  "environment": "*",
  "entrypoints": {
  },
  "mixins": [
  ],
  "depends": {
    "fabric-api": "*",
    "fabricloader": "${loader_version_fabric}",
    "minecraft": "${mc_version_fabric}",
    "java": "${java_version_fabric}"
  },
  "custom": {
    "modmenu": {
      "badges": [ ]
    }
  }
}
